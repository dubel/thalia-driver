# Empty lot

Top-down, **top of this doc = +Z = north**.

```
                    +Z north
                         ^
                         |
                         |
     west  --------------+--------------  east +X
                         |
                    spawn (0, 0)
                    yaw 0 (nose +Z)
                         |
                    south
```

Playable square: **±168 m** (`ARENA_HALF`), same as Karaluch. Surface is a gentle empty lot (asphalt/grass mix) with a painted grid so speed is readable. A low curb marks the rim; driving past it is blocked.

No roads, village, chapel, or pond yet. Keep landmarks off this square until they have entries here and in `config.ts`.
